import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: [
    'soccer-sage-anne-headset.trycloudflare.com',
    'constructed-right-weddings-ability.trycloudflare.com',
  ],
};

export default nextConfig;
